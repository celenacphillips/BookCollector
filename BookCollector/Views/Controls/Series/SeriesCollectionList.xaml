<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BookCollector.Views.Controls.Series.SeriesCollectionList"
             xmlns:localization="clr-namespace:BookCollector.Resources.Localization"
             xmlns:viewModel="clr-namespace:BookCollector.ViewModels.BaseViewModels"
             xmlns:controls="clr-namespace:BookCollector.Views.Controls.Series"
             x:DataType="viewModel:SeriesBaseViewModel">

    <CollectionView ItemsSource="{Binding FilteredSeriesList}"
                    SelectionMode="Single"
                    SelectedItem="{Binding SelectedSeries}"
                    SelectionChangedCommand="{Binding SeriesSelectionChangedCommand}"
                    ItemSizingStrategy="MeasureAllItems"
                    HeightRequest="{Binding CollectionViewHeight}">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <VerticalStackLayout>
                    <controls:SeriesCollectionItem/>
                </VerticalStackLayout>
            </DataTemplate>
        </CollectionView.ItemTemplate>

        <CollectionView.ItemsLayout>
            <LinearItemsLayout Orientation="Vertical"
                               SnapPointsType="Mandatory"
                               SnapPointsAlignment="Start" />
        </CollectionView.ItemsLayout>

        <CollectionView.EmptyView>
            <ContentView>
                <VerticalStackLayout HorizontalOptions="Center"
                                     HeightRequest="200"
                                     VerticalOptions="Start">
                    <Label Text="{x:Static localization:AppStringResources.ThereAreNoSeries}"
                           Margin="10,25,10,10"
                           HorizontalTextAlignment="Center"
                           Style="{StaticResource SubTitle}"/>
                </VerticalStackLayout>
            </ContentView>
        </CollectionView.EmptyView>
    </CollectionView>

</ContentView>
